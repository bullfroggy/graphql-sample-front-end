import { Routes } from '@angular/router';
import { GraphqlTestComponent } from './graphql-test/graphql-test.component';

export const routes: Routes = [{ path: '', component: GraphqlTestComponent }];
